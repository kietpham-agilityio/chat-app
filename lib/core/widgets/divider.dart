import 'package:flutter/material.dart';

class CADivider extends StatelessWidget {
  const CADivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(height: 0);
  }
}
